*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build/
/captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.keystore
!debug.keystore
output.json

# Python tooling artifacts (dev-only experiments, never shipped)
__pycache__/
*.pyc
*.pkl
.venv/

# User Python workspace — read-only, never stage from here (rule since v0.7).
# (22 engine-reference files already tracked remain visible to git.)
/tools/
